include: package:lints/recommended.yaml

analyzer:
  exclude:
    # Deliberately vulnerable code samples for rule tests and the demo.
    - test/fixtures/**
    - example/vulnerable_app/**

linter:
  rules:
    - prefer_single_quotes
    - unawaited_futures
